#db
spring.liquibase.enabled=true
spring.liquibase.url=${spring.datasource.url}
spring.liquibase.user=${spring.datasource.username}
spring.liquibase.password=${spring.datasource.password}
spring.liquibase.change-log=classpath:liquibase-changelog.yml

spring.jpa.hibernate.ddl-auto=none


#spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate.SQL=DEBUG

# field-level encryption
identity.encryption.secret=test-secret
identity.encryption.salt=test-salt


#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=${IDENTITY_SERVICE_MAIL_USERNAME:}
#spring.mail.password=${IDENTITY_SERVICE_MAIL_PASSWORD:}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#
#identity.email.from=${spring.mail.username}
